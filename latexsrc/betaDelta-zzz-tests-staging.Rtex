\documentclass{article}

\input{latexsrc/preamble/preamble}

%% begin.rcode
% #| label = "root",
% #| include = FALSE
% root <- rprojroot::is_rstudio_project
%% end.rcode

%% begin.rcode
% #| label = "knitr",
% #| include = FALSE,
% #| file = root$find_file(
% #|   "latexsrc",
% #|   "r-scripts",
% #|   "knitr.R"
% #| )
%% end.rcode

%% begin.rcode
% #| label = "biblatex",
% #| echo = FALSE,
% #| warning = FALSE,
% #| results = "asis"
% cat(
%   .PreambleBiblatexCombined(
%     path = root$find_file(
%       "latexsrc",
%       "bib"
%     ),
%     output_path = root$find_file(
%       "latexsrc"
%     )
%   )
% )
%% end.rcode

\renewcommand{\baselinestretch}{1}

\title{betaDelta: Staging}
\author{Ivan Jacob Agaloos Pesigan}
\date{}

\begin{document}

\maketitle

Staging...

%% begin.rcode
% object <- lm(QUALITY ~ NARTIC + PCTGRT + PCTSUPP, data = nas1982)
%% end.rcode

%% begin.rcode
% BetaDelta(object, type = "mvn")
% BetaDelta(object, type = "adf")
% out <- BetaDelta(object, type = "mvn")
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
% out <- BetaDelta(object, type = "adf")
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
%% end.rcode

%% begin.rcode
% BetaDelta(object, type = "mvn")
% BetaDelta(object, type = "adf")
% out <- rsq(BetaDelta(object, type = "mvn"))
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
% out <- rsq(BetaDelta(object, type = "adf"))
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
%% end.rcode

%% begin.rcode
% BetaDelta(object, type = "mvn")
% BetaDelta(object, type = "adf")
% out <- dif(BetaDelta(object, type = "mvn"))
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
% out <- dif(BetaDelta(object, type = "adf"))
% print(out)
% summary(out)
% coef(out)
% vcov(out)
% confint(out)
%% end.rcode

\nocite{Lib-R-Manual-2022}

\printbibliography

\end{document}
